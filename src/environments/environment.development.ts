// Development environment (used by `ng serve`).
// `/api` is forwarded to the backend by the Angular dev-server proxy
// (proxy.conf.json), which targets http://localhost:8080 by default.
export const environment = {
  production: false,
  apiBaseUrl: '/api',
};
